name: Delete

on:
  delete:

  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    if: github.event.ref_type == 'branch'
    steps:
    - name: Print github.event.ref
      run: |
          echo ${{ github.event.ref }}
